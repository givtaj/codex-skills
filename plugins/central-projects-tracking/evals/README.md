# Evaluation set

**central-projects-tracking.json** is the versioned golden request set for activation boundaries and workflow invariants.

The initial structural result is recorded under **results/2026-08-27-structural.json**. Host-level replay remains pending, so version 0.1.0 stays a public preview.

Replay the unchanged request ids on every supported host before stable promotion. For positive website cases, verify the finalized snapshot, all four views, search and filters, drawers, both copy actions, Git change plans, keyboard behavior, responsive layout, source-boundary copy, offline behavior, and site-manifest validation. Record observed activation, safety invariants, output contract, host, and any deviation. Do not rewrite expected behavior to match a failed run; change one instruction or metadata field at a time.
