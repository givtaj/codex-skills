schema_version=1
repository=self
# Allowed: discover, disabled
project_authority=discover
# Comma-separated subset of: core,intent,tasks,outcomes,planning,status,issues,projects,review,ci
surfaces=core
# Allowed: advisory, standard, strict
strictness=advisory
